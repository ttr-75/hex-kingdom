PORT=2567
NODE_ENV=development

# Database
DATABASE_URL=postgresql://hex_user:hex_pass_dev@localhost:5432/hex_kingdom

# Redis
REDIS_URL=redis://localhost:6379

# Game Settings
TICK_RATE=10
MAX_PLAYERS_PER_ROOM=10

# Time Multiplier (1.0 = normal speed, 10.0 = 10x faster, 20.0 = 20x faster)
# Affects: unit movement, building construction, research, unit recruitment
# Set higher in dev mode for faster testing
TIME_MULTIPLIER=1.0
